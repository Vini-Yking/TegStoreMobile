import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    backgroundColor: "#006400",
    paddingVertical: 5,
    paddingHorizontal: 7,
    borderRadius: 15,
  },
});
